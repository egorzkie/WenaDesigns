import React from "react";
import StripeContainer from "../Stripe/StripeContainer";

const Checkout = () => {
  return (
    <div className="App">
      <StripeContainer />
    </div>
  );
};

export default Checkout;